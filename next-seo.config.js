const title = "Super sklep internetowy";
const description = "Super sklep internetowy";

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    title,
    description,
    site_name: "Super sklep"
  }
}

export default nextSeoConfig;